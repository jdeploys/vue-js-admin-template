export enum UrlName {
  Auth = 'Auth',
  AuthSignIn = 'AuthSignIn',
  Base = 'Base',
  Dashboard = 'Dashboard',
  Setting = 'Setting'
}

export enum UrlDef {
  Root = '/',
  Auth = '/auth',
  AuthSignIn = '/auth/signIn',
  Dashboard = '/dashboard',
  Setting = '/setting'
}
